name: Publish to NPM

on:
  # Runs on every push to main
  push:
    branches:
      - main

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  check-and-publish:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [22.x]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Need full history to check tags

      - name: Check if should publish
        id: check_version
        run: |
          # Get the current version from package.json
          current_version=$(node -p "require('./package.json').version")
          echo "current_version=$current_version" >> "$GITHUB_OUTPUT"

          # Extract package name from package.json
          package_name=$(node -p "require('./package.json').name")
          echo "package_name=$package_name" >> "$GITHUB_OUTPUT"

          # Check if a tag exists for this version
          if git rev-parse "v$current_version" >/dev/null 2>&1; then
            echo "tag_exists=true" >> "$GITHUB_OUTPUT"
            echo "should_publish=false" >> "$GITHUB_OUTPUT"
            echo "::notice title=Tag Status::Tag v$current_version already exists - skipping publish"
          else
            echo "tag_exists=false" >> "$GITHUB_OUTPUT"

            # Check if the current commit is a version bump from the release workflow
            commit_message=$(git log -1 --pretty=%B)
            if [[ "$commit_message" =~ ^chore:\ bump\ version\ to\ .+ ]]; then
              echo "should_publish=true" >> "$GITHUB_OUTPUT"
              echo "is_version_bump=true" >> "$GITHUB_OUTPUT"
              echo "::notice title=Ready to Publish::Version $current_version - version bump commit detected"
            else
              echo "should_publish=false" >> "$GITHUB_OUTPUT"
              echo "is_version_bump=false" >> "$GITHUB_OUTPUT"
              echo "::notice title=Skipping Publish::Not a version bump commit from release workflow"
            fi
          fi

      - name: Exit if should not publish
        if: steps.check_version.outputs.should_publish != 'true'
        run: |
          if [[ "${{ steps.check_version.outputs.tag_exists }}" == "true" ]]; then
            echo "Tag v${{ steps.check_version.outputs.current_version }} already exists. Skipping."
          else
            echo "Not a version bump commit. Skipping."
          fi
          exit 0

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: npm ci

      - name: Build Shared UI
        run: npm run build:lib

      - name: 🏷️ Create and push tag
        if: steps.check_version.outputs.tag_exists != 'true'
        id: create_tag
        run: |
          version="${{ steps.check_version.outputs.current_version }}"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Create the tag
          git tag -a "v$version" -m "Release v$version"

          # Push the tag
          git push origin "v$version"

          echo "tag_name=v$version" >> "$GITHUB_OUTPUT"
          echo "tag_created=true" >> "$GITHUB_OUTPUT"
          echo "::notice title=Tag Created::Created and pushed tag v$version"

      - name: Get commit history for release
        id: get_commits
        run: |
          # Get the previous tag
          current_version="${{ steps.check_version.outputs.current_version }}"
          current_tag="v$current_version"

          # Get list of all tags sorted by version, excluding the current one
          previous_tag=$(git tag -l "v*" | grep -v "^$current_tag$" | sort -V | tail -n1 || echo "")

          if [ -z "$previous_tag" ]; then
            echo "No previous tag found"
            commits=$(git log --pretty=format:"- %s (%h)" --max-count=20)
          else
            echo "Previous tag: $previous_tag"
            commits=$(git log "$previous_tag"..HEAD --pretty=format:"- %s (%h)")
          fi

          # Prepare release body
          {
            echo "## 📦 Package Published to NPM"
            echo ""
            echo "Version \`$current_version\` has been published to [npm](https://www.npmjs.com/package/${{ steps.check_version.outputs.package_name }})."
            echo ""
            echo "### 📝 Changes in this release"
            echo ""
            echo "$commits"
            echo ""
            echo "### 📥 Installation"
            echo ""
            echo "\`\`\`bash"
            echo "npm install ${{ steps.check_version.outputs.package_name }}@$current_version"
            echo "\`\`\`"
          } > release_body.txt

          # Set the body as output
          body=$(cat release_body.txt)
          echo "body<<EOF" >> $GITHUB_OUTPUT
          echo "$body" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.check_version.outputs.current_version }}
          name: v${{ steps.check_version.outputs.current_version }}
          body: ${{ steps.get_commits.outputs.body }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish package to NPM
        id: npm_publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish --access public

      - name: Summary
        if: always()
        run: |
          {
            echo "## 📊 Publish Workflow Summary"
            echo ""
            echo "- **Package Version:** ${{ steps.check_version.outputs.current_version }}"
            echo "- **Package Name:** ${{ steps.check_version.outputs.package_name }}"
            echo "- **Version Bump Commit:** ${{ steps.check_version.outputs.is_version_bump || 'N/A' }}"
            echo "- **Tag Created:** ${{ steps.create_tag.outputs.tag_created == 'true' && 'Yes' || 'No (already existed)' }}"
            echo "- **GitHub Release Created:** Yes"
            echo "- **Published to NPM:** Yes"
            echo ""
            echo "✅ **Status:** Published successfully"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: 🧹 Cleanup on failure
        if: failure() && steps.create_tag.outputs.tag_created == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Workflow failed. Cleaning up created tag and release..."
          tag_name="${{ steps.create_tag.outputs.tag_name }}"

          # Delete the GitHub release if it exists
          if gh release view "$tag_name" >/dev/null 2>&1; then
            if gh release delete "$tag_name" --yes; then
              echo "✅ Successfully deleted GitHub release: $tag_name"
            else
              echo "⚠️ Failed to delete GitHub release: $tag_name"
            fi
          else
            echo "ℹ️ No GitHub release found for tag: $tag_name"
          fi

          # Delete the remote tag
          if git push --delete origin "$tag_name" 2>/dev/null; then
            echo "✅ Successfully deleted remote tag: $tag_name"
          else
            echo "⚠️ Failed to delete remote tag: $tag_name (it may not exist)"
          fi
